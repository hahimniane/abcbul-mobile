import 'package:flutter/material.dart';

class ProposalPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Text('Proposal Page'),
    );
  }
}
